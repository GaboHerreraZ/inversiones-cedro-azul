import React from "react";

export const Footer = React.memo(() => {
  return (
    <footer className="bg-footer bg-center bg-cover h-home">
      <div className="bg-black bg-opacity-70 h-full"></div>
    </footer>
  );
});
